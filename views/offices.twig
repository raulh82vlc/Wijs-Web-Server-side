{% extends 'layout.twig' %}
{% block body %}
<h1>Offices</h1>
    {% for office in offices %}
        <li><a href={{  app.url_generator.generate('office', {id: office.id}) }}>
        {{ office.street|e}}, {{ office.city|e}}</a><br>
        <br>
    {% endfor %}
{% endblock %}